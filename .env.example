# Pillar Brief environment example
#
# Credentials can also be added inside Settings. Environment variables are useful
# for self-hosted deployments, containers, or desktop development.

# Server
HOST=127.0.0.1
PORT=5173
PILLAR_APP_MODE=web
PILLAR_DATA_DIR=./data
# PILLAR_DB_PATH=./data/pillar-brief.sqlite

# Model providers
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=
XAI_API_KEY=

# Google Calendar OAuth.
# Packaged desktop builds use the hosted broker so the Google client secret is
# never bundled in the app. Self-hosted/custom deployments can set their own
# client ID + secret to use the direct local callback flow instead.
# PILLAR_GOOGLE_CALENDAR_AUTH_BROKER_URL=https://auth.pillar.transformationagency.com
# PILLAR_GOOGLE_CALENDAR_CLIENT_ID=
# PILLAR_GOOGLE_CALENDAR_CLIENT_SECRET=

# Custom OpenAI-compatible provider
CUSTOM_MODEL_API_KEY=
# CUSTOM_MODEL_BASE_URL=http://localhost:11434/v1

# Optional podcast transcription override
# OPENAI_TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe
# FFMPEG_PATH=/opt/homebrew/bin/ffmpeg

# Optional local speech-to-text with whisper.cpp
# WHISPER_CPP_PATH=/opt/whisper.cpp/build/bin/whisper-cli
# WHISPER_MODEL_PATH=/opt/whisper.cpp/models/ggml-tiny.en.bin
# WHISPER_MODEL_NAME=tiny.en
